run/
*.log
.DS_Store
native/DeepShell.app/

# SearXNG runtime copy with live secret_key — regenerated from the template
# by `deepshell.sh search install`; only settings.yml.template is committed.
search/settings/settings.yml
